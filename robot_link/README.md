# robot_link

Shared **envelope** and **payload** types for the GLaDOS split (Pi `pi_runtime` ↔ laptop `brain_runtime`).

Import from both sides:

```python
from robot_link import Envelope, HeartbeatPayload
```
