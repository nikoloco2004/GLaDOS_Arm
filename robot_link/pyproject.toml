[project]
name = "robot-link"
version = "0.1.0"
description = "Shared JSON message types for Pi ↔ brain WebSocket link"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
